/* let db = require("../database/models")
function listMiddleware(req, res, next) {
    
	if (req.session.userLogged && customer_type === 'Vendedor')   {
		return res.redirect('/home');
	}
	next();
}

module.exports = listMiddleware;  */